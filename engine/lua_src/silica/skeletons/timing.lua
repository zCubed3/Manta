-- SKELETON, IGNORE ME!
-- Timing is a bit strange!
-- Since there was no apparent way to index a module table, we've defined time(), delta_time(), and such as functions and not variables!

timing = {
    time = function ()
        return 0.0
    end,

    delta_time = function ()
        return 0.0
    end
}